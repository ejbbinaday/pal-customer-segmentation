# Development / CI tooling — NOT required at runtime.
# Runtime deps live in requirements.txt (used for Streamlit Cloud).
#   pip install -r requirements-dev.txt
#   pre-commit install
ruff>=0.8
bandit[toml]>=1.8
pre-commit>=4.0
