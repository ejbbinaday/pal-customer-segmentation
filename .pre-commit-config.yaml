# Pre-commit hooks for the PAL Customer Segmentation project.
# Setup:  pip install -r requirements-dev.txt && pre-commit install
# Run on everything once:  pre-commit run --all-files
# Bump hook versions:  pre-commit autoupdate
# Generated deliverables & deck sources are never touched by hooks.
exclude: ^(reports|assets)/
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ["--maxkb=1024"]
        # Tracked deck deliverables / reports are intentionally large. `docs/*.html` is the same
        # class of artifact: self-contained presenter guides that inline their own PNGs so they
        # open anywhere with no asset directory, which puts them at 1-3 MB by construction.
        # Narrowed to .html so an accidentally-committed large file elsewhere in docs/ still trips.
        exclude: ^(reports|assets)/|^docs/.*\.html$

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.22
    hooks:
      - id: ruff            # lint (auto-fix what it safely can)
        args: ["--fix"]
      - id: ruff-format     # formatter

  - repo: https://github.com/PyCQA/bandit
    rev: 1.9.4
    hooks:
      - id: bandit
        args: ["-c", "pyproject.toml"]
        additional_dependencies: ["bandit[toml]"]
